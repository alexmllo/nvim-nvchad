return {
    -- Symbol used for indentation
    symbol = "│",
    -- Highlight group for the scope character
    highlight = "IblScopeChar",
    -- Options for the indentscope
    options = {
        try_as_border = true, -- Display the scope as a border if possible
    },
    -- Additional options or settings if needed
}
